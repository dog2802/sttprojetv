import os

# Должно быть выставлено до первого импорта huggingface_hub (переменную окружения он читает
# один раз при импорте модуля с константами). __init__.py пакета гарантированно выполняется
# раньше любого его подмодуля - неважно, запущено ли через `python -m sttprojetv` или из
# собранного .exe (run.py). См. stt.ensure_model_downloaded - без xet прогресс скачивания
# модели один понятный байтовый прогресс-бар с заранее известным размером, а не несколько
# параллельных xet-стадий (download/reconstruct) с разной, плохо агрегируемой семантикой.
os.environ.setdefault("HF_HUB_DISABLE_XET", "1")

__version__ = "0.1.0"
